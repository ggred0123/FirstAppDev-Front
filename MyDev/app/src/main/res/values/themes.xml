<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MyDev" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.MyDev" parent="Base.Theme.MyDev" />

    <style name="Theme.YourAppTheme" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Status bar color -->
        <item name="android:statusBarColor">@android:color/white</item>

        <!-- 상태바의 아이콘을 밝은 색상으로 설정 (검은 배경일 경우) -->
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>